@model KRAM1.Models.Picture

@{
    ViewBag.Title = "FullImage";
}

<div class="container-narrow">
    <div class="row">
        <div class="col-sm-6 col-xs-6 box">
            <img src="@ViewBag.showpic.PicUrl" alt="Picture coming soon" class="img" />
            @*<p>@ViewBag.showpic.Description</p>*@
        </div>
    </div>
</div>

